{
  "name": "smartcyl-mqtt-relay",
  "version": "1.0.0",
  "description": "Cloud relay server that subscribes to SmartCyl's MQTT feed and re-serves live sensor data over REST + WebSocket, so the ESP32 never needs to accept inbound connections.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mqtt": "^5.10.1",
    "ws": "^8.18.0"
  }
}
